@model IEnumerable<OnlineShop.ViewModels.DishViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="product-container">
    <h1>Welcome to Sivach Shop!</h1>
    
    <div class="product-container">
        <div class="column">
            @foreach (var product in Model)
            {
                <div class="product-card">
                    <h2>@product.Name</h2>
                    <p>@product.Description</p>
                    <p class="price">Price: @product.Price @product.Currency</p>
                    <a href="@Url.Action("Details", "Dish", new { id = product.Id })" class="details-link">View Details</a>
                </div>
            }
        </div>
    </div>
</div>
