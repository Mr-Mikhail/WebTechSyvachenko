@model List<OnlineShop.ViewModels.DishViewModel>

<h1>Product List</h1>

@if (Model.Count > 0)
{
    <table class="table">
        <thead>
        <tr>
            <th>Product Name</th>
            <th>Product Description</th>
            <th>Product Price</th>
            <th>Currency</th>
            <th>Edit Product</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Description</td>
                <td>@product.Price</td>
                <td>@product.Currency</td>
                <td>
                    <a href="@Url.Action("Edit", "DishManagementSystem", new { id = product.Id })">Edit</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else
{
    <p>No products available.</p>
}